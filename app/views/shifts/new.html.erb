<div class="shift new">
  <h2>Create Shift</h2>

  <%= form_for(@shift) do |f| %>

    <%= render(:partial => 'form', :locals => {:f => f}) %>

    <div class="form-buttons">
      <%= f.submit("Create Shift") %>
    </div>

  <% end %>
</div>
